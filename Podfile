# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'elec' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for elec

  target 'elecTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'elecUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  pod 'PopupDialog', '~> 0.5'
  
  #Pods for Add the Charts
  #platform :ios, '8.0'
  pod ‘Charts’
  
  
  pod 'Toast-Swift', '~> 2.0.0'
  
  
  pod 'GaugeView'

end
